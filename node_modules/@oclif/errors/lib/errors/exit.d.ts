import { CLIError, OclifError } from '@oclif/errors/lib/errors/cli';
export declare class ExitError extends CLIError implements OclifError {
    oclif: {
        exit: number;
    };
    code: string;
    constructor(exitCode?: number);
    render(): string;
}
